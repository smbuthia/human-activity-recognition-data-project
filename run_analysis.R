## merges the training and test data sets to create one data set
mergeall <- function() {
    
}

## extracts only the measurements on the mean
getmeanmeasurements <- function() {
    
}

## extracts only the measurements on the standard deviation
getsdmeasurements <- function() {
    
}